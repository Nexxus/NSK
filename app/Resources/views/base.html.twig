<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Nexxus Stock Keeping{% endblock %}</title>
        
        <link href="{{ asset('bootstrap/stylesheet/bootstrap.css') }}" rel="stylesheet"/>
        <link href="{{ asset('stylesheet/override_style.css') }}" rel="stylesheet"/>
        
        {% block stylesheets %}
            
        {% endblock %}
            
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block navbar %}
        <nav class="navbar navbar-default navbar-gradient">
          <div class="container-fluid">
            <div class="container">
                <div class="navbar-header">
                  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                  </button>
                 <!-- <a class="navbar-brand" href="#"><img height="28" src="{{ asset('images/logos/header-logoGlow.png') }}"></a> -->
                </div>
                <div class="navbar-collapse collapse" id="mainMenu">
                    {% block navlist %}
                      {{ knp_menu_render('AppBundle:Builder:createMainMenu', {'currentClass': 'active', 'allow_safe_labels': true}) }}
                    {% endblock %}
               
                {% if app.user %}
                 <ul class="nav navbar-nav navbar-right">
                     <li><a href="{{ logout_path() }}">Logout</a></li>
                 </ul>
                {% else %}
                    <ul class="nav navbar-nav navbar-right">
                     <li><a href="{{ path("fos_user_security_login") }}">Login</a></li>
                    </ul>
                {% endif %}
                 </div>
            </div>
          </div>
        </nav>
        {% endblock %}
        <div class="container">
            
            {% block breadcrumb %}
            {% endblock %}
            
            {% block body %}
            <h2>Nexxus Stock Keeping. Het OpenSource voorraad beheer systeem.</h2>
			<p>A great way to keep track of your Stock and Inventory. You can also use this software as a Point of Sales, for Logistics and Distribution and even for Supplier and Client Management. <br>
			Nexxus Stock Keeping works with existing barcodes, or it can generate a barcode for you when you create a new product. You can use any reference to create the barcode.
			</p><br>
			<p>Nexxus Stock Keeping is software van Copiatek. De software is uitgegeven onder GPL en is Open Source. 
			</p>
            {% endblock %}
            
        </div>
        <script src="{{ asset('bootstrap/js/jquery.min.js') }}"></script>
        <script src="{{ asset('bootstrap/js/bootstrap.min.js') }}"></script>
        {% block javascripts %}
          
        {% endblock %}
    </body>
</html>
