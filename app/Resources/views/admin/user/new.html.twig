{% extends 'base.html.twig' %}

{% block body %}
    <h2>{% trans %}Create new User{% endtrans %}</h2>
    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}