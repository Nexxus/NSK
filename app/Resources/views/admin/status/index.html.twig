{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% trans %}Status List{% endtrans %}</h1>
    <h4><a href="{{ path('status_create') }}">{% trans %}Create a new Status{% endtrans %}</a></h4>
    {% if info is defined %}
    Bing
    {% endif %}
    <table class="table table-striped">
        <tr>
            <th>Index</th>
            <th>{% trans %}Name{% endtrans %}</th>
            <th>&nbsp;</th>
        </tr>
    {% for a in productstatus %}
        <tr>
            <td>{{ a.pindex }}</td>
            <td>{{ a.name }}</td>
            <td>
                <a href="{{ path("status_edit", {id: a.id}) }}">
                    <button type="button" class="btn btn-default">
                        {% trans %}Edit{% endtrans %}
                    </button></a>
                <a href="{{ path("status_delete", {id: a.id, pindex: a.pindex}) }}">
                    <button type="button" class="btn btn-danger" href="#">
                        {% trans %}Delete{% endtrans %}
                    </button></a>
            </td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}