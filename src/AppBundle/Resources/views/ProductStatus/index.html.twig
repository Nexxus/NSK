{% extends 'admin.html.twig' %}

{% block body %}

    <div class="row" style="margin-bottom: 20px">
        <div class="col-sm-9">
            <h1>Product status list</h1>
        </div>
        <div class="col-sm-3" style="padding-top: 15px">
            <a href="{{ path('productstatus_edit', { 'id': 0 }) }}" class="btn btn-primary pull-right" role="button">Create new status</a>
        </div>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Sort</th>
                <th width="1%"></th>
            </tr>
        </thead>
    {% for s in productStatuses %}
        <tr>
            <td>{{ s.id }}</td>
            <td>{{ s.name }}</td>
            <td>{{ s.pindex }}</td>
            <td nowrap>
                <a class="btn btn-success" href="{{ path('productstatus_edit', {'id': s.id}) }}">Edit</a>
            </td>
        </tr>
        {% else %}
            <tr>
                <td colspan="99">
                    <h4>{% trans %}No records found{% endtrans %}</h4>
                </td>
            </tr>
    {% endfor %}
{% endblock %}
    
